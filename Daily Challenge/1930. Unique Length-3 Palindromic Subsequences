int countPalindromicSubsequence(char* s) 
{
    int len = strlen(s);

    int map[26][26] = {{0}};
    int count = 0;
    for(int i = 0; i < len - 2; i++)
    {
        for(int j = i + 2; j < len; j++)
        {
            if(s[i] == s[j])
            {
                for(int k = i + 1; k < j; k++)
                {
                    if(!map[s[i] - 'a'][s[k] - 'a'])
                    {
                        map[s[i] - 'a'][s[k] - 'a'] = 1;
                        count++;
                    }
                    else
                    {
                        continue;
                    }
                }
            }
            else
            {
                continue;
            }
        }
    }
    return count;

    
}
